
pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id("com.android.application") version "8.5.2"
        id("org.jetbrains.kotlin.android") version "1.9.24"
    }
} // 빌드도구를 어디서, 어떤 버전으로 쓸지에 대한 내용-관리
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
} // 앱코드에 사용하는 라이브러리 관리
rootProject.name = "android-storage-bench-min"
include(":app")

// 프로젝트 이름과 :app 모듈 사용을 선언
